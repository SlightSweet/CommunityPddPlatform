server:
  port: 8083

spring:
  application:
    name: group-product-service
  datasource:
    url: jdbc:mysql://localhost:3306/pdd_product?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  redis:
    host: localhost
    port: 6379
    database: 0
    timeout: 2000ms

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: product.pojo
